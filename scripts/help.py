#!/usr/bin/env python3
"""SuperTuTu — 帮助 / 快速参考卡。

用法:
    python help.py

不需要 API Key，不发网络请求。直接打印一份"我在微信里该怎么问"的指令样例清单。

何时调：
    用户问「图图怎么用？」「帮助」「/help」「快速参考」等触发词时，
    Claude 调本脚本输出指令样例给用户，比手写解释更快更整齐。
"""
import sys

HELP_TEXT = """\
# 🦞 SuperTuTu 图图 — 快速参考卡

在微信里跟龙虾说话，下面这些指令我都能听懂。

---

## 📚 看资源

| 你说 | 我会做 |
|---|---|
| 「看下我的空间」/「我有什么空间」 | 查询「我的空间」列表（按更新时间倒序）|
| 「看下漫画风格」/「有哪些治愈风」 | 列出可用的漫画风格 |
| 「看下配图风格」 | 列出可用的文章配图风格 |
| 「看下我最近的作品」/「我之前做过什么」 | 分页列出最近作品 |

---

## 🎨 做漫画（推荐先用空间）

| 你说 | 我会做 |
|---|---|
| 「用[XX]空间做个漫画，故事是…」 | 用你指定的空间生成分镜，**先让你 review 文案** |
| 「用我最常用的空间做漫画」 | 查空间列表 → 列出来让你挑 |
| 「帮我做个治愈漫画，故事是…」 | 先查空间有没有匹配的，没有就用治愈风默认参数 |
| 「帮我做个 4 格趣味漫画」 | 自动用气泡对话形态（趣味类默认气泡）|
| 「直接出图，不用看分镜」 | 跳过精修，一把梭生图 |

---

## ✏️ 精修分镜（生图前）

```
我已经把分镜列给你看了，你可以这么改：

「第 2 格字幕改成『连泡面都嫌弃我』」
「第 3 格气泡换成 [我] AAA、[泡面] BBB」
「第 1 格提示词加 cinematic lighting」
「第 4 格不要字幕了」
「第 3 格清空气泡」
「重新生成全部分镜」
「开始生图」/「全部满意」/「没问题」 ← 触发出图
「算了不要了」/「取消」 ← 终止，workId 留着稍后继续
```

**精修不扣积分**——改五遍跟一遍同价。文案是漫画的灵魂，使劲改。

---

## 🖼️ 配图 / 自定义生图

| 你说 | 我会做 |
|---|---|
| 「给这篇文章配 4 张图」+ 文章正文 | 文章配图（≥300 字，自动选风格）|
| 「画一张图，描述是…」 | 自定义生图（直接 prompt → 图，最快）|

---

## 🚦 默认行为

- **漫画默认走分步精修**：先把每格字幕 / 气泡列给你看，确认了再生图（除非你说"直接出图"）
- **优先用空间**：每次创作前先查「我的空间」，按名字匹配
- **默认带字幕**：不指定输出形态时默认 `split`（画面+字幕），让 LLM 真的生成 caption 供你审
- **治愈类自动字幕，趣味类自动气泡**：按风格的 `defaultLayout` 推

---

## 💰 积分账单

| 动作 | 积分 |
|---|---|
| 起分镜（提示词）| 1 |
| 改字幕 / 气泡 / 提示词 | 0（免费）|
| 生图（每张）| 2 |
| 文章配图 | 1（提示词）+ N × 2（生图）|
| 自定义生图 | 2 |

漫画 4 格典型 = 9 积分；改文案不另收钱。

---

## 🆘 遇到问题

- API Key 没填 / 失效 → 去 https://sso.aizmjx.com/home/apikey 重新领
- 积分不足 → 同上链接充值
- 生图超过 5 分钟没回 → 拿 workId 去 https://tutu.aizmjx.com/gallery 查
- 想用空间但还没建过 → 去 https://tutu.aizmjx.com/workspace 建一个（锁定常用风格）

---

## 🧰 完整脚本列表（高级用户）

```
list_workspaces.py     查询「我的空间」（漫画首选入口）
create_comic.py        漫画生成（推荐 --workspace-id）
create_prompt.py       仅生成分镜（分步精修入口，推荐 --workspace-id）
update_shot.py         单格精修字幕 / 气泡 / 提示词
render_work.py         用最新分镜续接生图
create_article_illustration.py    文章配图
create_image.py        自定义生图
list_styles.py         查询可用风格
list_works.py          查询历史作品
help.py                本帮助卡
```

完整 API 参考见仓库 [API.md](https://github.com/aizmjx/tutu-skill/blob/main/API.md)。
"""


def main() -> None:
    print(HELP_TEXT)


if __name__ == "__main__":
    main()
